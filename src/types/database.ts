import { Theme, Store, StoreFaq, Product, ProductVariant, DomainStatus, SizeType, Unit } from "@prisma/client";

export type {
  Theme,
  Store,
  StoreFaq,
  Product,
  ProductVariant,
  DomainStatus,
  SizeType,
  Unit,
};
